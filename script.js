// Logic comes in Version 2 — for now this just confirms the button works.
document.getElementById('checkBtn').addEventListener('click', () => {
  alert('Button works! Matching logic comes in Version 2.');
});
